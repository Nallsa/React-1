import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <div class="header">
    <h1>
      <span class="red-text">Hello, intocode!</span>
    </h1>
  </div>,

  document.getElementById('root')
);
